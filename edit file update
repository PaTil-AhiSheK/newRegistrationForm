<!DOCTYPE html>
<html lang="en">
<head>
    {% load static %}
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Employee</title>
    <link rel="stylesheet" href="{% static 'css/style.css' %}">
</head>
<body>
    <div class="container">
        <h1>Edit Employee</h1>

        <!-- Back to Dashboard Button -->
        <div class="navigation">
            <a href="/myapp/dashboard/" class="btn">Back to Dashboard</a>
        </div>

        <!-- Edit Employee Form -->
        <form method="POST" action="/myapp/edit/" class="employee-form">
            {% csrf_token %}
            <input type="hidden" name="id" value="{{ employee.id }}">

            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="{{ employee.name }}" required>
            </div>

            <div class="form-group">
                <label for="age">Age:</label>
                <input type="number" id="age" name="age" value="{{ employee.age }}" required>
            </div>

            <div class="form-group">
                <label for="unit">Unit:</label>
                <input type="text" id="unit" name="unit" value="{{ employee.unit }}" required>
            </div>

            <button type="submit" class="btn">Update Employee</button>
        </form>
    </div>
</body>
</html>